# ===================================
# CONFIGURAÇÃO DE PRODUÇÃO
# ===================================

# Database (Supabase)
# Obtenha estas URLs no painel do Supabase > Settings > Database
DATABASE_URL="postgresql://postgres.[REF]:[PASSWORD]@aws-1-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.[REF]:[PASSWORD]@aws-1-us-east-1.compute-1.amazonaws.com:5432/postgres"

# JWT - GERE UMA CHAVE FORTE E ÚNICA!
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="GERE-UMA-CHAVE-FORTE-AQUI-64-CARACTERES-MINIMO"
JWT_EXPIRES_IN="7d"

# Server
PORT=3001
NODE_ENV=production

# Frontend URL (para CORS) - ALTERE PARA SEU DOMÍNIO
FRONTEND_URL=https://seu-dominio.com

# OpenAI API (opcional - para funcionalidade de IA)
# Se não for usar, deixe vazio
OPENAI_API_KEY=

# ===================================
# INSTRUÇÕES
# ===================================
# 1. Copie este arquivo para .env
# 2. Substitua todos os valores entre [COLCHETES]
# 3. Gere um JWT_SECRET forte
# 4. Configure o FRONTEND_URL com seu domínio real
# 5. NUNCA commite o arquivo .env no Git!
